name: Build Linux

on:
  workflow_dispatch:
    inputs:
      checkout_ref:
        description: "Explicit git ref or SHA to build instead of the workflow ref"
        required: false
        type: string
        default: ""
      package_installer:
        description: "Build and upload the native .deb installer"
        required: false
        type: boolean
        default: true
      artifact_retention_days:
        description: "Retention for uploaded installer artifacts"
        required: false
        type: number
        default: 3
      version_override:
        description: "Explicit version to use instead of deriving from the git ref"
        required: false
        type: string
        default: ""
  workflow_call:
    inputs:
      checkout_ref:
        description: "Explicit git ref or SHA to build instead of the workflow ref"
        required: false
        type: string
        default: ""
      package_installer:
        description: "Build and upload the native .deb installer"
        required: false
        type: boolean
        default: false
      artifact_retention_days:
        description: "Retention for uploaded installer artifacts"
        required: false
        type: number
        default: 3
      version_override:
        description: "Explicit version to use instead of deriving from the git ref"
        required: false
        type: string
        default: ""
    outputs:
      version:
        description: 'Build version'
        value: ${{ jobs.build.outputs.version }}

jobs:
  build:
    name: Build & Package Linux
    concurrency:
      group: >-
        ${{ github.event_name == 'workflow_call' &&
            format('call-{0}-{1}-{2}', github.run_id, github.run_attempt, matrix.identifier) ||
            format('{0}-{1}-{2}', github.workflow, github.ref_name, matrix.identifier) }}
      cancel-in-progress: true
    runs-on: [self-hosted, Linux, pioneer-compile]
    env:
      JULIA_NUM_THREADS: auto
    outputs:
      version: ${{ steps.get_version.outputs.version }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x64
            identifier: linux-x64

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: ${{ inputs.checkout_ref || github.sha }}

      - name: Determine VERSION
        id: get_version
        uses: ./.github/actions/determine-version
        with:
          fallback: "dev"
          override: ${{ inputs.version_override }}

      - name: Normalize versions
        id: norm
        uses: ./.github/actions/normalize-versions
        with:
          raw: ${{ steps.get_version.outputs.version }}

      - name: Setup Julia
        uses: julia-actions/setup-julia@v3
        with:
          version: '1.12'

      - name: Cache Julia packages
        uses: actions/cache@v5
        with:
          path: |
            ~/.julia/registries
            ~/.julia/packages
          key: julia-linux-${{ matrix.identifier }}-${{ hashFiles('**/Project.toml','**/Manifest.toml') }}
          restore-keys: julia-linux-${{ matrix.identifier }}-

      - name: Install dependencies
        run: |
          julia --threads auto --project=. -e 'using Pkg; Pkg.instantiate()'
          julia --threads auto --project=dev -e 'using Pkg; Pkg.instantiate()'

      - name: Precompile data (cache & download)
        uses: ./.github/actions/precompile-data
        with:
          cache-key: precompile-data-v1

      - name: Compile application
        run: |
          export LIGHTGBM_LOG_LIBRARY_DISCOVERY=false
          export PIONEER_APP_CPU_TARGET="generic;sandybridge,-xsaveopt,clone_all;haswell,-rdrnd,base(1)"
          export JULIA_DEPOT_PATH="$RUNNER_TEMP/julia-depot:$HOME/.julia"
          mkdir -p "$RUNNER_TEMP/julia-depot"
          julia --threads auto --project=dev -e '
            using PackageCompiler;
            include(joinpath(pwd(), "src", "build", "runtime_bundle_utils.jl"));
            root = "build/Pioneer_${{ matrix.identifier }}/Applications/Pioneer";
            apps_root = joinpath(root, "apps");
            mkpath(apps_root);

            create_app(
              "apps/PioneerParamsApp",
              joinpath(apps_root, "params"),
              incremental=true, force=true,
              cpu_target=ENV["PIONEER_APP_CPU_TARGET"],
              executables=[
                "GetSearchParams"=>"main_GetSearchParams",
                "GetBuildLibParams"=>"main_GetBuildLibParams",
                "GetParseSpecLibParams"=>"main_GetParseSpecLibParams"
              ]
            );

            create_app(
              "apps/PioneerPredictApp",
              joinpath(apps_root, "predict"),
              incremental=true, force=true,
              cpu_target=ENV["PIONEER_APP_CPU_TARGET"],
              include_transitive_dependencies=false,
              executables=["BuildSpecLib"=>"main_BuildSpecLib"]
            );
            bundle_and_instantiate_runtime_project!(
              "apps/PioneerPredictApp",
              joinpath(apps_root, "predict")
            );

            create_app(
              "apps/PioneerSearchApp",
              joinpath(apps_root, "search"),
              incremental=true, force=true,
              cpu_target=ENV["PIONEER_APP_CPU_TARGET"],
              include_transitive_dependencies=false,
              executables=["SearchDIA"=>"main_SearchDIA"]
            );
            bundle_and_instantiate_runtime_project!(
              "apps/PioneerSearchApp",
              joinpath(apps_root, "search")
            );

            create_app(
              "apps/PioneerConvertApp",
              joinpath(apps_root, "convert"),
              incremental=true, force=true,
              cpu_target=ENV["PIONEER_APP_CPU_TARGET"],
              executables=["convertMzML"=>"main_convertMzML"]
            );
          '

      - name: Smoke test bundled runtime packages
        shell: bash
        run: |
          search_share="build/Pioneer_${{ matrix.identifier }}/Applications/Pioneer/apps/search/share/julia"
          predict_share="build/Pioneer_${{ matrix.identifier }}/Applications/Pioneer/apps/predict/share/julia"

          JULIA_LOAD_PATH="$search_share:$search_share/dev:@stdlib" JULIA_DEPOT_PATH="$search_share" JULIA_PKG_PRECOMPILE_AUTO=0 \
          julia --startup-file=no --pkgimages=no --project="$search_share" -e '
            Base.require(Base.PkgId(Base.UUID("9adec8df-eb60-4bac-9b0a-4137287d3bbd"), "PioneerPlots"));
            Base.require(Base.PkgId(Base.UUID("05f169b2-1f58-4f3e-a5d2-7bfdd2b4f8e3"), "PioneerSIMD"));
            @assert Base.identify_package("PioneerPlots") !== nothing;
            @assert Base.identify_package("PioneerSIMD") !== nothing;
          '

          JULIA_LOAD_PATH="$predict_share:$predict_share/dev:@stdlib" JULIA_DEPOT_PATH="$predict_share" JULIA_PKG_PRECOMPILE_AUTO=0 \
          julia --startup-file=no --pkgimages=no --project="$predict_share" -e '
            Base.require(Base.PkgId(Base.UUID("05f169b2-1f58-4f3e-a5d2-7bfdd2b4f8e3"), "PioneerSIMD"));
            @assert Base.identify_package("PioneerSIMD") !== nothing;
          '

      - name: Download PioneerConverter
        shell: bash
        run: |
          mkdir -p converter
          PATTERN="${{ matrix.identifier }}"
          ASSET=$(curl -fsSL \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${{ github.token }}" \
            https://api.github.com/repos/nwamsley1/PioneerConverter/releases/latest \
            | jq -r --arg PATTERN "$PATTERN" '.assets[]
              | select(.name|test("PioneerConverter-"+$PATTERN+".*\\.zip"))
              | .name' | head -n1)

          if [[ -z "$ASSET" ]]; then
            echo "Error: ASSET not found"
            exit 1
          fi

          curl -L "https://github.com/nwamsley1/PioneerConverter/releases/latest/download/$ASSET" \
            -o converter.zip
          unzip -q converter.zip -d converter
          mkdir -p "build/Pioneer_${{ matrix.identifier }}/Applications/Pioneer/bin"
          cp -r "converter/PioneerConverter-${PATTERN}/bin/"* \
            "build/Pioneer_${{ matrix.identifier }}/Applications/Pioneer/bin/"
          mkdir -p "build/Pioneer_${{ matrix.identifier }}/Applications/Pioneer/lib"
          cp -r "converter/PioneerConverter-${PATTERN}/lib/"* \
            "build/Pioneer_${{ matrix.identifier }}/Applications/Pioneer/lib/"

      - name: Add wrapper scripts
        shell: bash
        run: |
          cp src/build/CLI/pioneer \
            build/Pioneer_${{ matrix.identifier }}/Applications/Pioneer/
          chmod +x build/Pioneer_${{ matrix.identifier }}/Applications/Pioneer/pioneer

      - name: Populate bundled app data
        shell: bash
        run: |
          pioneer_root="build/Pioneer_${{ matrix.identifier }}/Applications/Pioneer"
          mkdir -p "$pioneer_root/data"
          cp -r assets/* "$pioneer_root/data/"
          for subapp in params predict search; do
            mkdir -p "$pioneer_root/apps/$subapp/data"
            cp -r assets/* "$pioneer_root/apps/$subapp/data/"
          done

      - name: Write bundled version
        shell: bash
        run: |
          pioneer_root="build/Pioneer_${{ matrix.identifier }}/Applications/Pioneer"
          echo "${{ steps.get_version.outputs.version }}" > "$pioneer_root/VERSION"
          for subapp in params predict search convert; do
            echo "${{ steps.get_version.outputs.version }}" > "$pioneer_root/apps/$subapp/VERSION"
          done

      - name: Pre-download artifacts
        shell: bash
        run: |
          pioneer_root="build/Pioneer_${{ matrix.identifier }}/Applications/Pioneer"
          dummy="$pioneer_root/dummy"
          mkdir -p "$dummy"
          "$pioneer_root/pioneer" params-predict "$dummy" "$dummy" --params-path "$dummy/params.json"
          rm -rf "$dummy"
          
      - name: Package Linux .deb
        if: startsWith(github.ref, 'refs/tags/v') || inputs.package_installer
        run: |
          mkdir -p deb/usr/local/Pioneer
          cp -r build/Pioneer_${{ matrix.identifier }}/Applications/Pioneer/* deb/usr/local/Pioneer/
          mkdir -p deb/usr/local/bin
          ln -sf ../Pioneer/pioneer deb/usr/local/bin/pioneer
          find deb/usr/local/Pioneer -path '*/share/julia' -exec chmod -R a+rwx {} + || true
          info=src/build/package_info.json
          RAW_VERSION='${{ steps.get_version.outputs.version }}'
          BASE_PACKAGE=$(jq -r '.package' $info)
          PACKAGE="$BASE_PACKAGE"
          MAINTAINER=$(jq -r '.maintainer' $info)
          DESCRIPTION=$(jq -r '.description' $info)
          DEB_VERSION='${{ steps.norm.outputs.deb }}'
          if [[ "$RAW_VERSION" == dev-* ]]; then
            PACKAGE="${BASE_PACKAGE}-dev"
            CONFLICTS="$BASE_PACKAGE"
            REPLACES="$BASE_PACKAGE"
          else
            CONFLICTS="${BASE_PACKAGE}-dev"
            REPLACES="${BASE_PACKAGE}-dev"
          fi

          mkdir -p deb/DEBIAN
          cat <<EOF > deb/DEBIAN/control
          Package: $PACKAGE
          Version: $DEB_VERSION
          Architecture: amd64
          Maintainer: $MAINTAINER
          Provides: $BASE_PACKAGE
          Conflicts: $CONFLICTS
          Replaces: $REPLACES
          Description: $DESCRIPTION
          EOF

          dpkg-deb --build --root-owner-group deb \
            Pioneer-${{ steps.get_version.outputs.version }}-${{ matrix.identifier }}.deb

      - name: Upload .deb
        if: startsWith(github.ref, 'refs/tags/v') || inputs.package_installer
        uses: actions/upload-artifact@v7
        with:
          name: Pioneer-${{ steps.get_version.outputs.version }}-${{ matrix.identifier }}-deb
          path: Pioneer-${{ steps.get_version.outputs.version }}-${{ matrix.identifier }}.deb
          retention-days: ${{ inputs.artifact_retention_days }}
