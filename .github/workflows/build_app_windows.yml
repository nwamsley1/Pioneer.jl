name: Build Windows

on:
  workflow_dispatch:
    inputs:
      checkout_ref:
        description: "Explicit git ref or SHA to build instead of the workflow ref"
        required: false
        type: string
        default: ""
      package_installer:
        description: "Build and upload the native MSI installer"
        required: false
        type: boolean
        default: true
      artifact_retention_days:
        description: "Retention for uploaded installer artifacts"
        required: false
        type: number
        default: 3
      version_override:
        description: "Explicit version to use instead of deriving from the git ref"
        required: false
        type: string
        default: ""
  workflow_call:
    inputs:
      checkout_ref:
        description: "Explicit git ref or SHA to build instead of the workflow ref"
        required: false
        type: string
        default: ""
      package_installer:
        description: "Build and upload the native MSI installer"
        required: false
        type: boolean
        default: false
      artifact_retention_days:
        description: "Retention for uploaded installer artifacts"
        required: false
        type: number
        default: 3
      version_override:
        description: "Explicit version to use instead of deriving from the git ref"
        required: false
        type: string
        default: ""
    outputs:
      version:
        description: 'Build version'
        value: ${{ jobs.build.outputs.version }}

jobs:
  build:
    name: Build & Package Windows
    concurrency:
      group: >-
        ${{ github.event_name == 'workflow_call' &&
            format('call-{0}-{1}-{2}', github.run_id, github.run_attempt, matrix.identifier) ||
            format('{0}-{1}-{2}', github.workflow, github.ref_name, matrix.identifier) }}
      cancel-in-progress: true
    runs-on: [self-hosted, Windows, X64, compile]
    env:
      JULIA_NUM_THREADS: auto
    outputs:
      version: ${{ steps.get_version.outputs.version }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x64
            identifier: windows-x64

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 1
          ref: ${{ inputs.checkout_ref || github.sha }}

      - name: Determine VERSION
        id: get_version
        uses: ./.github/actions/determine-version
        with:
          fallback: "dev"
          override: ${{ inputs.version_override }}

      - name: Normalize versions
        id: norm
        uses: ./.github/actions/normalize-versions
        with:
          raw: ${{ steps.get_version.outputs.version }}

      # Temporary. The last two Windows runs lost the runner during Compile
      # application and uploaded NO logs, so nothing is known about the
      # machine's real limits. Artifacts upload independently of the
      # end-of-job log push, so writing this to a file at minute two survives
      # a runner that dies at minute 150.
      - name: Probe runner limits
        shell: pwsh
        run: |
          $out = "$env:RUNNER_TEMP\runner-probe.txt"
          "=== physical memory ===" | Out-File $out
          Get-CimInstance Win32_ComputerSystem |
            Select-Object @{n='TotalRAM_GB';e={[math]::Round($_.TotalPhysicalMemory/1GB,2)}},
                          AutomaticManagedPagefile | Format-List | Out-File $out -Append
          "=== pagefile setting ===" | Out-File $out -Append
          Get-CimInstance Win32_PageFileSetting |
            Select-Object Name, InitialSize, MaximumSize | Format-List | Out-File $out -Append
          "=== pagefile in use ===" | Out-File $out -Append
          Get-CimInstance Win32_PageFileUsage |
            Select-Object Name, AllocatedBaseSize, PeakUsage | Format-List | Out-File $out -Append
          "=== virtual memory totals ===" | Out-File $out -Append
          Get-CimInstance Win32_OperatingSystem |
            Select-Object @{n='TotalVisible_GB';e={[math]::Round($_.TotalVisibleMemorySize/1MB,2)}},
                          @{n='TotalVirtual_GB';e={[math]::Round($_.TotalVirtualMemorySize/1MB,2)}} |
            Format-List | Out-File $out -Append
          "=== volumes ===" | Out-File $out -Append
          Get-CimInstance Win32_LogicalDisk -Filter "DriveType=3" |
            Select-Object DeviceID,
                          @{n='Size_GB';e={[math]::Round($_.Size/1GB,1)}},
                          @{n='Free_GB';e={[math]::Round($_.FreeSpace/1GB,1)}} |
            Format-Table | Out-File $out -Append
          "TEMP=$env:TEMP"               | Out-File $out -Append
          "RUNNER_TEMP=$env:RUNNER_TEMP" | Out-File $out -Append
          Get-Content $out

      # NOTE: an "Enlarge the pagefile" step lived here. It was written for the
      # disposable 16 GB hosted runner, where the codegen exhausted the 2.9 GB
      # default pagefile. #449 moves this job to CB-GOLDFARB-01, a physical lab
      # machine, where that step would disable automatic pagefile management and
      # force a 32-64 GB pagefile permanently. Removed with the runner change.
      # It never fixed the build anyway -- see the cpu_target note below.

      - name: Upload the probe
        uses: actions/upload-artifact@v7
        with:
          name: windows-runner-probe
          path: ${{ runner.temp }}/runner-probe.txt
          retention-days: 3

      - name: Setup Julia
        uses: julia-actions/setup-julia@v3
        with:
          version: '1.12.6'  # must satisfy Project.toml's julia = "1.12.5" compat
          arch: ${{ matrix.arch }}

      - name: Cache Julia packages
        uses: actions/cache@v5
        with:
          path: |
            C:\Users\runneradmin\.julia\registries
            C:\Users\runneradmin\.julia\packages
          key: julia-windows-${{ matrix.identifier }}-${{ hashFiles('**/Project.toml','**/Manifest.toml') }}
          restore-keys: julia-windows-${{ matrix.identifier }}-

      - name: Install dependencies
        shell: bash
        run: |
          julia --threads auto --startup-file=no --project=. \
            -e 'using Pkg; Pkg.instantiate()'
          julia --threads auto --startup-file=no --project=packaging/compiler \
            -e 'using Pkg; Pkg.instantiate()'

      - name: Validate locked application environment
        shell: bash
        run: |
          export LIGHTGBM_LOG_LIBRARY_DISCOVERY=false
          julia --threads auto --startup-file=no --project=. \
            packaging/check_app_contract.jl
          julia --threads auto --startup-file=no --project=. \
            src/build/check_cpu_dependencies.jl
          git diff --exit-code -- \
            Manifest.toml packaging/compiler/Manifest.toml

      - name: Precompile data (cache & download)
        uses: ./.github/actions/precompile-data
        with:
          cache-key: precompile-data-v2-yeast

      # Before create_app, because a dead statements file is cheap to detect and expensive to ship.
      # PackageCompiler skips unresolvable statements silently, so drift would otherwise be invisible.
      - name: Check precompile statements
        shell: bash
        run: |
          export LIGHTGBM_LOG_LIBRARY_DISCOVERY=false
          julia --threads auto --startup-file=no --project=. \
            src/build/check_statements.jl

      - name: Compile application
        shell: bash
        run: |
          export LIGHTGBM_LOG_LIBRARY_DISCOVERY=false
          unset PIONEER_APP_CPU_TARGET
          fresh_depot="$(mktemp -d)"
          export JULIA_DEPOT_PATH="$(cygpath -w "$fresh_depot");$(cygpath -w "$HOME/.julia")"
          julia --threads auto --startup-file=no --project=packaging/compiler \
            packaging/build_app.jl \
            "build/Pioneer_${{ matrix.identifier }}/Applications/Pioneer/"

      - name: Swap in MSVC LightGBM DLL (Windows core-utilization fix)
        shell: pwsh
        run: |
          # The mingw-w64 LightGBM binary Julia bundles barely threads the tree-learner
          # FIT (~1 core, libgomp-on-winpthreads barrier cost). Replace it with Microsoft's
          # MSVC build of the SAME version (3.3.5) - it saturates all cores with
          # bit-identical, seeded/deterministic results. Full write-up + benchmarks:
          # src/build/windows/LIGHTGBM_MSVC.md.
          # PIN COUPLING: these hashes match LightGBM = "2.2" (=> C API of LightGBM 3.3.5).
          # If that compat is bumped, update these hashes AND
          # src/utils/install/windows_lightgbm.jl to the matching MSVC build.
          $ErrorActionPreference = 'Stop'
          $wheelUrl = 'https://files.pythonhosted.org/packages/6a/31/13f80e5abac627c53375c1dc49404d622d929272a231c05b2f4f7bb98b9e/lightgbm-3.3.5-py3-none-win_amd64.whl'
          $wheelSha = '02a40745c1972cf4a2cde764c7739228f45178c2237af2df40fde7063a58ac6a'
          $dllSha   = 'a52a9494c894f9987f9be24c97e0dec7c37fd23b1184aa0ac39d3200ffa51699'
          $app = "build\Pioneer_${{ matrix.identifier }}\Applications\Pioneer"

          # 1) download + verify the pinned wheel (a zip)
          Invoke-WebRequest -Uri $wheelUrl -OutFile lgbm.whl -UseBasicParsing
          $h = (Get-FileHash -Algorithm SHA256 lgbm.whl).Hash.ToLower()
          if ($h -ne $wheelSha) { throw "wheel SHA256 mismatch: got $h" }

          # 2) extract + verify the MSVC lib_lightgbm.dll
          Expand-Archive -LiteralPath lgbm.whl -DestinationPath lgbm_whl -Force
          $msvc = "lgbm_whl\lightgbm\lib_lightgbm.dll"
          $h2 = (Get-FileHash -Algorithm SHA256 $msvc).Hash.ToLower()
          if ($h2 -ne $dllSha) { throw "DLL SHA256 mismatch: got $h2" }

          # 3) overwrite the bundled (mingw) DLL in the app depot; fail loudly if the
          #    create_app layout changed and none was found (so this can never silently no-op)
          $targets = @(Get-ChildItem $app -Recurse -Filter lib_lightgbm.dll)
          if ($targets.Count -eq 0) { throw "no bundled lib_lightgbm.dll under $app - create_app layout changed?" }
          foreach ($t in $targets) { Copy-Item $msvc $t.FullName -Force; Write-Host "swapped: $($t.FullName)" }

          # 4) app-local VC++ runtime (vcomp140 = OpenMP) so the MSVC DLL loads with no
          #    separate redistributable install. Placed next to the executables and the DLL.
          $bin = Join-Path $app "bin"
          foreach ($vc in 'vcomp140.dll','vcruntime140.dll','msvcp140.dll') {
            $src = Join-Path $env:WINDIR "System32\$vc"
            if (-not (Test-Path $src)) { throw "VC runtime $vc not present on the build runner" }
            Copy-Item $src $bin -Force
            foreach ($t in $targets) { Copy-Item $src $t.Directory.FullName -Force }
          }
          Write-Host "MSVC LightGBM swap + app-local VC runtime complete."

      - name: Download PioneerConverter
        shell: pwsh
        run: |
          $dir = "converter"
          New-Item -ItemType Directory -Force -Path $dir | Out-Null
          $pattern = "win-x64"
          $headers = @{ Accept = 'application/vnd.github+json'; Authorization = 'Bearer ${{ github.token }}' }
          $json = Invoke-WebRequest -Uri https://api.github.com/repos/nwamsley1/PioneerConverter/releases/latest -Headers $headers |
                  Select-Object -ExpandProperty Content | ConvertFrom-Json
          $asset = ($json.assets | Where-Object { $_.name -like "*$pattern*.zip" } | Select-Object -First 1).name
          if (-not $asset) {
            Write-Error "Error: ASSET not found"
            exit 1
          }
          Invoke-WebRequest -Uri "https://github.com/nwamsley1/PioneerConverter/releases/latest/download/$asset" -OutFile converter.zip
          Expand-Archive -Path converter.zip -DestinationPath $dir
          $subdir = Get-ChildItem -Path $dir -Directory | Select-Object -First 1
          Copy-Item "$($subdir.FullName)\bin\*" "build\Pioneer_${{ matrix.identifier }}\Applications\Pioneer\bin\" -Recurse
          New-Item -ItemType Directory -Force -Path "build\Pioneer_${{ matrix.identifier }}\Applications\Pioneer\lib" | Out-Null
          Copy-Item "$($subdir.FullName)\lib\*" "build\Pioneer_${{ matrix.identifier }}\Applications\Pioneer\lib\" -Recurse

      - name: Add wrapper scripts
        shell: pwsh
        run: |
          Copy-Item src\build\CLI\pioneer.bat build\Pioneer_${{ matrix.identifier }}/Applications/Pioneer/

      - name: Write bundled version
        shell: pwsh
        run: |
          Set-Content -Path "build\Pioneer_${{ matrix.identifier }}\Applications\Pioneer\VERSION" `
                      -Value "${{ steps.get_version.outputs.version }}"

      - name: Pre-download artifacts
        shell: pwsh
        run: |
          $pioneerRoot = "build\Pioneer_${{ matrix.identifier }}\Applications\Pioneer"
          $dummy = "$pioneerRoot\dummy"
          New-Item -ItemType Directory -Force -Path $dummy | Out-Null
          & "$pioneerRoot\pioneer.bat" params-predict $dummy $dummy --params-path "$dummy\params.json"
          Remove-Item -Recurse -Force $dummy

      - name: Normalize version for MSI
        id: semver
        shell: pwsh
        run: |
          $raw = '${{ steps.get_version.outputs.version }}'

          # strip leading 'v' / 'V'
          $clean = $raw -replace '^[vV]', ''

          # drop prerelease/build metadata (e.g. -rc.1, +build)
          $clean = $clean.Split('+')[0]
          $clean = $clean.Split('-')[0]

          # pad/trim to 3–4 numeric fields for MSI (x.y.z[.w])
          $parts = $clean.Split('.')
          if ($parts.Count -lt 3) {
            $clean = ($parts + @('0','0'))[0..2] -join '.'
          } elseif ($parts.Count -gt 4) {
            $clean = $parts[0..3] -join '.'
          }

          "msi_version=$clean" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
          Write-Host "MSI_VERSION=$clean"

      # ---- GUI (gui/) -------------------------------------------------
      # windows-latest ships MSVC and the WebView2 runtime, so this needs no
      # extra system packages. The exe is copied into $pioneerRoot below, where
      # heat.exe harvests it automatically.
      - name: Setup Node (GUI)
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: gui/package-lock.json

      - name: Setup Rust (GUI)
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust (GUI)
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: gui/src-tauri

      - name: Build GUI
        working-directory: gui
        shell: pwsh
        run: |
          npm ci
          # --no-bundle: the bare exe is wanted, not Tauri's own MSI.
          npm run tauri:build -- --no-bundle

      - name: Package Windows MSI
        if: startsWith(github.ref, 'refs/tags/v') || inputs.package_installer
        shell: pwsh
        run: |
          $pioneerRoot = "build\Pioneer_${{ matrix.identifier }}\Applications\Pioneer"
          $dataDir = "$pioneerRoot\data"
          New-Item -ItemType Directory -Force -Path $dataDir | Out-Null
          Copy-Item assets\* $dataDir -Recurse
          Copy-Item "src\build\windows\LICENSE.rtf" "$pioneerRoot\LICENSE.rtf"

          # Placed in the install root (not bin\) so the MSI's PATH entry,
          # which points at INSTALLFOLDER, also covers the GUI. The Start Menu
          # shortcut in installer.wxs refers to this same location.
          $guiExe = "gui\src-tauri\target\release\pioneer-console.exe"
          if (Test-Path $guiExe) {
            Copy-Item $guiExe "$pioneerRoot\pioneer-gui.exe"
            Write-Host "GUI included in MSI"
          } else {
            Write-Host "GUI exe not found at $guiExe - packaging CLI only"
          }
          $artifactDir = "$pioneerRoot\share\julia\artifacts"
          if (Test-Path $artifactDir) {
            Get-ChildItem -Path $artifactDir -Recurse -Directory |
              Where-Object {
                $_.FullName -like "*QtQuick*" -or
                $_.FullName -like "*Qt6*"     -or
                $_.Name -eq "include"         -or
                $_.Name -eq "test"            -or
                $_.Name -eq ".qt"             -or
                $_.FullName.Length -gt 200
              } |
              Remove-Item -Recurse -Force -ErrorAction SilentlyContinue
          }
          $info = Get-Content src\build\package_info.json | ConvertFrom-Json
          $desc       = $info.description
          $identifier = $info.identifier
          $rawVersion = '${{ steps.get_version.outputs.version }}'
          Write-Host "MSI will replace any existing Pioneer install (version=$rawVersion)"

          & heat.exe dir $pioneerRoot `
            -cg ProductComponents `
            -dr INSTALLFOLDER `
            -platform x64 `
            -gg -scom -sreg -sfrag -srd `
            -var var.SourceDir `
            -out components.wxs

          & candle.exe `
            -dSourceDir="$pioneerRoot" `
            -dProductVersion="${{ steps.norm.outputs.msi }}" `
            -dProductDescription="$desc" `
            -dPackageIdentifier="$identifier" `
            -dPlatform="x64" `
            -arch x64 `
            -ext WixUIExtension `
            -ext WixUtilExtension `
            src\build\windows\installer.wxs components.wxs

          & light.exe `
            -ext WixUIExtension `
            -ext WixUtilExtension `
            -cultures:en-us `
            -sval `
            -out "Pioneer-${{ steps.get_version.outputs.version }}-${{ matrix.identifier }}.msi" `
            installer.wixobj components.wixobj

      - name: Upload MSI
        if: startsWith(github.ref, 'refs/tags/v') || inputs.package_installer
        uses: actions/upload-artifact@v7
        with:
          name: Pioneer-${{ matrix.identifier }}-msi
          path: Pioneer-${{ steps.get_version.outputs.version }}-${{ matrix.identifier }}.msi
          retention-days: ${{ inputs.artifact_retention_days }}
