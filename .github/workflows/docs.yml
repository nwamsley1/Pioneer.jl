name: Docs

on:
  push:
    branches:
      - main
      - develop
    tags:
      - 'v*'
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref_name }}
  cancel-in-progress: true

jobs:
  docs:
    name: Docs
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 1

      - uses: julia-actions/setup-julia@v3
        with:
          version: '1.12.6'  # match Project.toml's `julia = "1.12.5"` compat lower bound

      - name: Cache docs packages
        uses: actions/cache@v6
        with:
          path: |
            ~/.julia/registries
            ~/.julia/packages
          key: docs-${{ runner.os }}-${{ hashFiles('Project.toml','Manifest.toml','docs/Project.toml','docs/Manifest.toml') }}

      - name: Configure doc environment
        shell: julia --threads auto --project=docs --color=yes {0}
        run: |
          using Pkg
          Pkg.develop(path=pwd())
          Pkg.instantiate()

      - name: Build docs
        run: julia --threads auto --project=docs docs/make.jl

      - name: Deploy docs
        if: github.ref == 'refs/heads/develop' || startsWith(github.ref, 'refs/tags/v')
        uses: julia-actions/julia-docdeploy@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DOCUMENTER_KEY: ${{ secrets.DOCUMENTER_KEY }}

      - name: Run doctests
        shell: julia --threads auto --project=docs --color=yes {0}
        run: |
          using Documenter: DocMeta, doctest
          using Pioneer
          DocMeta.setdocmeta!(Pioneer, :DocTestSetup, :(using Pioneer); recursive=true)
          doctest(Pioneer)
