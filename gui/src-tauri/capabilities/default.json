{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the Pioneer console window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
